# Catalog assets use validators but must revalidate so an old successful browser
# cache entry cannot outlive a newer deployment.
/catalog.json
  Cache-Control: public, max-age=0, must-revalidate

/catalog-summary.json
  Cache-Control: public, max-age=0, must-revalidate
